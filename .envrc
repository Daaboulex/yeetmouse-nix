# Canonical .envrc -- Nix Packaging Standard. `use flake` via nix-direnv makes
# the dev shell's exports (incl. the .devshell/ state pins) reach every
# in-project entry point; run `direnv allow` once per clone to activate.
use flake
